/**
 * TIDElabs - El Velo del Pasado
 * Componente que aplica el efecto CRT (scanlines y phosphor glow) sobre toda la experiencia
 */

export function CRTEffect() {
  return <div className="crt-overlay" aria-hidden="true" />;
}
